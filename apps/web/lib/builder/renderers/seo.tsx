import type { BlockRenderer } from './types';

export const seoRenderers: Record<string, BlockRenderer> = {
  'meta-tags': ({ el, selectedId, selectElement, baseProps, children }) => {

  },
  'schema-markup': ({ el, selectedId, selectElement, baseProps, children }) => {

  },
};
