{
  "version": "1.1.0",
  "safety_rules": {
    "prevent_self_modification": true,
    "prevent_recursive_orchestration": true,
    "sandbox_execution_required": true
  },
  "capabilities": {
    "build_scope": ["playcanvas_scenes", "webgl_components", "ui_kits"],
    "allowed_tools": ["filesystem_write", "component_lookup", "schema_validation"]
  },
  "constraints": [
    "The AI shall not attempt to modify its own source code or orchestration logic.",
    "The AI must strictly adhere to the master-schema.json for all UI and 3D generation."
  ]
}