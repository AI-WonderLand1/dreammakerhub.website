apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wonderland-workspace-ingress
  namespace: wonderland
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/websocket-services: "wonderland-workspace"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
    - hosts:
        - "*.ide.dreammakerhub.website"
      secretName: dreammakerhub-wildcard-tls
  rules:
    - host: "*.ide.dreammakerhub.website"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: wonderland-workspace
                port:
                  number: 80