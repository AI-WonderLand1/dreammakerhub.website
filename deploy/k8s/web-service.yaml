apiVersion: v1
kind: Service
metadata:
  name: wonderland-web
  namespace: wonderland
  labels:
    app: wonderland-web
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: wonderland-web
