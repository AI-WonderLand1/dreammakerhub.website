name: Launch public endpoint diagnostics
on:
  pull_request:
    branches: [Master]
    paths:
      - '.github/scripts/public-endpoint-check.mjs'
      - '.github/scripts/public-endpoint-check.check.mjs'
      - '.github/workflows/launch-public-endpoints.yml'
  workflow_dispatch:
  schedule:
    - cron: '47 13 * * *'
permissions:
  contents: read
jobs:
  public-endpoints:
    runs-on: ubuntu-latest
    timeout-minutes: 7
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Unit test diagnostic (no network)
        run: node --test .github/scripts/public-endpoint-check.check.mjs
      - name: Check public endpoints from GitHub hosted VM
        run: node .github/scripts/public-endpoint-check.mjs
