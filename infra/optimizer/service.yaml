apiVersion: v1
kind: Service
metadata:
  name: optimizer-service
  namespace: default
spec:
  selector:
    app: optimizer
  ports:
    - protocol: TCP
      port: 3090
      targetPort: 3090
  type: ClusterIP
