apiVersion: v1
kind: Service
metadata:
  name: wonderplay-service
  namespace: WonderPlay
spec:
  selector:
    app: wonderplay
  ports:
    - name: http
      protocol: TCP
      port: 3090
      targetPort: 3090
  type: ClusterIP